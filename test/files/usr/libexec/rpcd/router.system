#!/bin/sh

. /usr/share/libubox/jshn.sh

case "$1" in
        list)
                echo '{ "processes" : {} }'
        ;;
        call)
                case "$2" in
                        processes)
                                cat /tmp/processes.data 2>/dev/null
                        ;;
                esac
        ;;
esac

